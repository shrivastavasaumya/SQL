

CREATE DATABASE db_name;
CREATE DATABASE IF NOT EXISTS db_name;      {if this DB is already created, then don't recreate the same DB}

DROP DATABASE db_name;
DROP DATABASE IF NOT EXISTS db_name;        {if given DB is not there, then this command will give "warning"}

SHOW DATABASES;                             {Shows all the DB's}

SHOW TABLES;                                {Shows tables in DB which we are using}